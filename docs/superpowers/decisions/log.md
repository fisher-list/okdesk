# okdesk 决策与沟通记录 (Decision Log)

本文档用于记录与老铁确认的关键开发决策，确保开发过程的一致性。

---

## 2026-04-03 - 核心架构与功能定型

### 0. 项目信息
- **项目名称**：okdesk
- **源码管理**：GitHub (待配置)

### 1. 核心技术选型
- **开发语言**：原生 **Rust** (方案一)。
- **传输协议**：**WebRTC** (webrtc-rs)，支持 P2P 直连。
- **中继服务器**：已安装 (hbbr/turn)，用于防火墙穿透。
- **音视频处理**：强制开启**硬件加速** (Windows: DXGI/NVENC, macOS: ScreenCaptureKit/VideoToolbox)。

### 2. 核心功能交互
- **多显示器支持**：采用 **B 方案 (切换模式)**。支持通过顶部菜单或快捷键在多个远程屏幕间秒切。
- **文件传输**：采用 **B 方案 (文件管理器)**。使用双栏布局进行管理，支持大文件断点续传。
- **连接方式**：
  - **临时密码**：6 位随机字符，支持手动/自动刷新。
  - **永久密码**：支持手动设置固定密码，便于个人在不同设备间长期互连。
  - **申请模式**：ID 发起申请，客户端手动点击接受。
- **性能目标**：端到端延时 **< 1s**，画面清晰流畅。

### 4. 联调与验收
- **测试策略**：先通过 CLI 版本在 Mac 和 Windows 间跑通信令与基础握手逻辑，验证“延迟 < 1s”的潜力。
- **验收标准**：见 `docs/superpowers/plans/testing_guide.md`。
- **打包计划**：测试通过后，使用 `cargo-bundle` 和 `cargo-wix` 生成 DMG/EXE 安装包。

---
*注：本文件由 Accio 自动维护，记录老铁的每一项关键指令。*
